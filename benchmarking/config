# Index of the CPU on which the NF will run
DUT_CPU=8
# PCI BDF addresses of the DUT's NICs
DUT_DEV_0=83:00.0
DUT_DEV_1=85:00.0

# Hostname of the tester
TESTER_HOST=icnalsp3s4
# PCI BDF addresses of the tester's NICs
TESTER_DEV_0=83:00.0
TESTER_DEV_1=85:00.0
